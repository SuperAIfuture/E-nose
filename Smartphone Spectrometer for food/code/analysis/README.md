# Public analysis code

`public_spectral_algorithms.py` contains reusable preprocessing, validation, regression, classification, and window-occlusion routines for the released article data tables. 
